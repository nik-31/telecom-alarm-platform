spring:
  application:
    name: alarm-ingestion-service
  datasource:
    url: jdbc:mysql://localhost:3306/telecom_alarm_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

  flyway:
    enabled: true